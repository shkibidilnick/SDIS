# Лабораторная работа №2
## по дисциплине «Проектирование программного обеспечения интеллектуальных систем»

**Тема:** разработка оконного приложения с графическим интерфейсом на Python  
**Вариант:** 8  
**Предметная область:** учёт питомцев и ветеринарных приёмов

---

## 1. Цель работы

Целью лабораторной работы является изучение принципов построения графического пользовательского интерфейса и разработка программной системы с GUI на языке Python в соответствии с заданием.

В рамках работы необходимо было разработать приложение, которое:
- построено по шаблону **Model-View-Controller**;
- имеет **главное окно** и **дочерние диалоги**;
- позволяет **добавлять**, **искать**, **удалять**, **отображать**, **сохранять** и **загружать** записи;
- хранит записи в **базе данных**;
- поддерживает экспорт и импорт данных в **XML**;
- показывает записи как в виде **таблицы**, так и в виде **дерева**;
- поддерживает **постраничный вывод**;
- покрыта **unit-тестами**.

---

## 2. Постановка задачи

Согласно варианту 8, каждая запись содержит следующие поля:
- имя питомца;
- дата рождения;
- дата последнего приёма;
- ФИО ветеринара;
- диагноз.

Необходимо реализовать поиск и удаление записей по следующим условиям:
1. по имени питомца и дате рождения;
2. по дате последнего приёма и ФИО ветеринара;
3. по фразе из диагноза.

Также приложение должно обеспечивать:
- ввод записей через отдельное диалоговое окно;
- вывод всех текущих записей в главном окне;
- вывод результатов поиска в отдельном диалоге поиска;
- удаление записей через отдельный диалог удаления;
- уведомление пользователя о результате удаления;
- сохранение и загрузку записей из XML-файла;
- хранение данных в базе SQLite;
- альтернативное отображение данных в виде дерева;
- постраничный вывод записей.

---

## 3. Использованные технологии

При разработке системы использовались следующие средства:
- **Python 3.13** — язык программирования;
- **PySide6** — библиотека для создания графического пользовательского интерфейса;
- **SQLite** — встроенная база данных для хранения записей;
- **xml.dom.minidom** — для записи XML-документа через DOM;
- **xml.sax** — для чтения XML-документа через SAX;
- **unittest** — модуль для написания unit-тестов;
- **coverage** — инструмент для измерения покрытия кода тестами;
- **PyCharm** — среда разработки.

---

## 4. Архитектура приложения

Приложение реализовано по шаблону **MVC (Model-View-Controller)** с пассивной Model.

### 4.1. Model

Слой Model включает:
- доменные сущности;
- критерии поиска и удаления;
- валидаторы;
- пагинацию;
- работу с репозиторием;
- XML-импорт и XML-экспорт.

Model не зависит от графического интерфейса и содержит логику предметной области.

### 4.2. View

Слой View включает:
- главное окно программы;
- диалог добавления записи;
- диалог поиска;
- диалог удаления;
- таблицу записей;
- дерево записей;
- виджет пагинации;
- сообщения пользователю.

View отвечает только за отображение данных и ввод информации пользователем.

### 4.3. Controller

Слой Controller связывает интерфейс и бизнес-логику:
- обрабатывает действия пользователя;
- вызывает методы сервисов;
- обновляет представление после операций добавления, поиска, удаления, импорта и экспорта.

---

## 5. Структура проекта

```text
Lab2_Sem4/
├── app/
│   ├── controllers/     # контроллеры главного окна и диалогов
│   ├── domain/          # сущности, критерии, валидаторы, пагинация
│   ├── repositories/    # SQLite-репозиторий
│   ├── services/        # сервисы приложения
│   ├── views/           # окна, диалоги, таблица, дерево, пагинация
│   ├── xmlio/           # экспорт DOM и импорт SAX
│   └── main.py          # точка входа
├── sample_data/         # демонстрационные XML-файлы
├── tests/               # unit-тесты
├── requirements.txt     # зависимости проекта
└── README.md            # отчёт по разработанной системе
```

---

## 6. Описание основных модулей

### 6.1. Доменные сущности

В системе используются основные сущности:
- `PersonName` — ФИО ветеринара;
- `PetRecord` — запись о питомце и последнем приёме.

Для корректности ввода реализованы проверки:
- имя питомца не должно быть пустым;
- диагноз не должен быть пустым;
- дата рождения не может быть позже текущей даты;
- дата последнего приёма не может быть раньше даты рождения;
- ФИО ветеринара должно быть задано корректно.

### 6.2. Репозиторий

Для хранения данных используется репозиторий `SQLitePetRecordRepository`, который обеспечивает:
- создание таблицы в базе данных;
- добавление записей;
- получение всех записей;
- удаление записей по условиям варианта;
- поиск по условиям варианта;
- добавление записей из XML в текущий набор данных приложения.

### 6.3. Сервисы

В проекте реализованы сервисы:
- `PetRecordService` — добавление записей и получение страниц данных;
- `SearchService` — поиск записей;
- `DeleteService` — удаление записей;
- `ImportExportService` — импорт и экспорт XML.

### 6.4. XML-модуль

Для обмена данными реализованы:
- `PetRecordDomWriter` — запись XML через DOM;
- `PetRecordSaxReader` — чтение XML через SAX.

Это соответствует требованиям задания.

---

## 7. Функциональные возможности системы

Разработанное приложение поддерживает следующие функции:

### 7.1. Добавление записи
Пользователь открывает диалог добавления, вводит:
- имя питомца;
- дату рождения;
- дату последнего приёма;
- ФИО ветеринара;
- диагноз.

После подтверждения запись проходит валидацию, сохраняется в базе данных и отображается в главном окне.

### 7.2. Отображение записей
Главное окно показывает записи:
- в виде таблицы;
- в виде дерева, где каждая запись раскрывается по отдельным полям.

### 7.3. Поиск записей
В отдельном диалоге поиска пользователь может искать записи:
- по имени питомца и дате рождения;
- по дате последнего приёма и ФИО ветеринара;
- по фразе из диагноза.

Результаты поиска выводятся в этом же диалоговом окне.

### 7.4. Удаление записей
В отдельном диалоге удаления пользователь задаёт условия удаления:
- по имени питомца и дате рождения;
- по дате последнего приёма и ФИО ветеринара;
- по фразе из диагноза.

После удаления система сообщает пользователю, сколько записей удалено, либо сообщает, что записи не были найдены.

### 7.5. Работа с XML
Реализованы:
- экспорт текущих записей в XML-файл;
- импорт записей из XML-файла.

### 7.6. Пагинация
Для удобства просмотра записей реализован постраничный вывод с возможностью:
- перехода на первую страницу;
- перехода на предыдущую страницу;
- перехода на следующую страницу;
- перехода на последнюю страницу;
- изменения количества записей на странице.

---

## 8. Описание пользовательского интерфейса

Главное окно приложения содержит:
- строку меню;
- панель инструментов;
- таблицу записей;
- дерево записей;
- панель пагинации;
- строку состояния.

### Меню приложения
- **Файл**
  - Импорт XML
  - Экспорт XML
  - Выход
- **Записи**
  - Добавить
  - Поиск
  - Удалить
  - Обновить
- **Справка**
  - О программе

### Панель инструментов
Панель инструментов дублирует основные команды меню:
- добавление записи;
- поиск;
- удаление;
- обновление;
- импорт XML;
- экспорт XML.

---

## 9. Unit-тестирование

Для проверки корректности работы системы реализованы unit-тесты на базе `unittest`.

Покрыты следующие части проекта:
- пагинация;
- валидаторы;
- репозиторий SQLite;
- сервисы поиска, удаления, импорта и экспорта;
- XML DOM/SAX.

Тестирование графического интерфейса не являлось основной целью, поэтому основное внимание уделено тестированию прикладной логики и работы с данными.

### Результаты тестирования
- всего тестов: **18**;
- все тесты выполняются успешно;
- общее покрытие кода тестами: **96%**.

Пример запуска тестов:

```bash
python -m unittest discover -s tests -p "test_*.py" -v
coverage run -m unittest discover -s tests -p "test_*.py"
coverage report -m
```

---

## 10. Инструкция по запуску

### 10.1. Создание виртуального окружения

```bash
python3.13 -m venv .venv
source .venv/bin/activate
```

### 10.2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 10.3. Запуск приложения

```bash
python -m app.main
```

### 10.4. Запуск тестов

```bash
python -m unittest discover -s tests -p "test_*.py" -v
```

---

## 11. Сценарии демонстрации системы

Для демонстрации используются подготовленные XML-файлы:
- `sample_data/pets_batch_1.xml`
- `sample_data/pets_batch_2.xml`

Каждый файл содержит **55 уникальных записей**. При этом второй файл не повторяет первый: в нём используются другие клички питомцев, другие даты рождения и последнего приёма, другой набор ФИО ветеринаров и другие формулировки диагнозов. Для всех записей дата последнего приёма не превышает **05.04.2026**, поэтому наборы данных корректны для предметной области и подходят для демонстрации.

### Сценарий 1. Запуск программы и обзор интерфейса
После запуска приложения открывается главное окно системы учёта питомцев и ветеринарных приёмов. В окне последовательно демонстрируются:
- строка меню;
- панель инструментов;
- таблица записей;
- древовидное представление;
- панель пагинации;
- строка состояния.

Если база данных ещё не заполнена, таблица и дерево отображаются пустыми.

### Сценарий 2. Добавление записи вручную
Через команду **«Добавить»** открывается диалог ввода новой записи.  
В диалоге вводятся, например, следующие данные:
- имя питомца: **Барсик**
- дата рождения: **14.05.2021**
- дата последнего приёма: **10.03.2026**
- ФИО ветеринара: **Иванов Петр Сергеевич**
- диагноз: **Плановый осмотр, здоров**

После подтверждения запись сохраняется в базе данных и сразу появляется:
- в таблице главного окна;
- в древовидном представлении.

### Сценарий 3. Импорт первого демонстрационного файла
В главном окне выбирается команда **«Импорт XML»**, после чего указывается файл `sample_data/pets_batch_1.xml`.

После импорта в системе отображаются реальные записи из файла, например:
- **Барсик-1-1**, дата рождения **2017-01-01**, дата последнего приёма **2025-01-01**, ветеринар **Иванов Петр Сергеевич**, диагноз **«Плановый осмотр, здоров»**
- **Мурка-1-2**, дата рождения **2018-02-02**, дата последнего приёма **2025-04-06**, ветеринар **Соколова Анна Игоревна**, диагноз **«Вакцинация, состояние хорошее»**
- **Рекс-1-3**, дата рождения **2019-03-03**, дата последнего приёма **2025-07-11**, ветеринар **Петров Олег Викторович**, диагноз **«Аллергическая реакция, назначено лечение»**
- **Лайма-1-4**, дата рождения **2020-04-04**, дата последнего приёма **2025-10-16**, ветеринар **Кузнецова Мария Андреевна**, диагноз **«Проблемы с пищеварением, назначена диета»**
- **Боня-1-5**, дата рождения **2021-05-05**, дата последнего приёма **2025-01-21**, ветеринар **Иванов Петр Сергеевич**, диагноз **«Отит, проведена обработка»**

Этот сценарий показывает чтение XML через SAX и загрузку записей в базу данных приложения.

### Сценарий 4. Проверка постраничного вывода
После импорта файла `sample_data/pets_batch_1.xml`, содержащего 55 уникальных записей, в главном окне демонстрируется пагинация.

Проверяются:
- переход на следующую и предыдущую страницы;
- переход на первую и последнюю страницы;
- изменение числа записей на странице: **5, 10, 20, 50**.

Например, при выборе значения **5** на первой странице отображаются только первые 5 записей из файла, а при выборе **10** — уже 10 записей. Информационная строка показывает номер текущей страницы, число страниц и общее количество записей.

### Сценарий 5. Поиск по имени питомца и дате рождения
Открывается диалог **«Поиск»**, выбирается условие **по имени питомца и дате рождения**.

Вводятся значения:
- имя питомца: **Барсик-1-1**
- дата рождения: **2017-01-01**

После выполнения поиска система находит запись:
- **Барсик-1-1**
- дата последнего приёма: **2025-01-01**
- ветеринар: **Иванов Петр Сергеевич**
- диагноз: **Плановый осмотр, здоров**

Результат отображается в самом диалоге поиска.

### Сценарий 6. Поиск по дате последнего приёма и ФИО ветеринара
В диалоге поиска выбирается условие **по дате последнего приёма и ФИО ветеринара**.

Задаются значения:
- дата последнего приёма: **2025-04-06**
- ФИО ветеринара: **Соколова Анна Игоревна**

После выполнения поиска находится запись:
- **Мурка-1-2**
- диагноз: **Вакцинация, состояние хорошее**

Этот сценарий демонстрирует второе условие поиска, предусмотренное вариантом 8.

### Сценарий 7. Поиск по фразе из диагноза
В диалоге поиска выбирается условие **по фразе из диагноза**.

Сначала вводится фраза:
- **лечение**

Система находит запись:
- **Рекс-1-3**
- диагноз: **Аллергическая реакция, назначено лечение**

Затем можно повторить поиск по фразе:
- **отит**

В этом случае находится запись:
- **Боня-1-5**
- диагноз: **Отит, проведена обработка**

### Сценарий 8. Удаление записей по условию
Открывается диалог **«Удаление»**. Выбирается условие удаления **по фразе из диагноза**.

В поле вводится фраза:
- **отит**

После подтверждения система удаляет запись:
- **Боня-1-5**
- диагноз: **Отит, проведена обработка**

После завершения операции выводится сообщение:
- **Удалено записей: 1**

Затем в главном окне показывается, что удалённая запись исчезла из таблицы и древовидного представления.

### Сценарий 9. Экспорт текущих данных в XML
После добавления или импорта записей выполняется команда **«Экспорт XML»**. Пользователь указывает имя выходного файла.

Система формирует XML-документ с текущим набором записей и сохраняет его на диск.  
При демонстрации можно открыть сохранённый файл и показать структуру XML-документа, сформированного DOM-парсером.

### Сценарий 10. Импорт второго демонстрационного файла
Для подтверждения работы приложения с несколькими наборами данных выполняется импорт файла `sample_data/pets_batch_2.xml`.

После загрузки в таблице отображаются записи второго набора, например:
- **Ася-2-1**, дата рождения **2017-04-06**, дата последнего приёма **2025-03-08**, ветеринар **Орлов Дмитрий Павлович**, диагноз **«Ревакцинация, состояние стабильное»**
- **Буран-2-2**, дата рождения **2018-07-11**, дата последнего приёма **2024-05-15**, ветеринар **Васильева Наталья Олеговна**, диагноз **«Легкое растяжение лапы, назначен покой»**
- **Веста-2-3**, дата рождения **2019-10-16**, дата последнего приёма **2025-07-22**, ветеринар **Мельников Артем Ильич**, диагноз **«Повышенная чувствительность кожи, рекомендован уход»**
- **Грета-2-4**, дата рождения **2020-01-21**, дата последнего приёма **2024-09-02**, ветеринар **Федорова Ксения Романовна**, диагноз **«Плановая стоматологическая обработка, без осложнений»**
- **Джой-2-5**, дата рождения **2021-04-26**, дата последнего приёма **2025-11-09**, ветеринар **Крылов Максим Евгеньевич**, диагноз **«Небольшое воспаление глаза, назначены капли»**

После импорта второго файла записи не заменяют первый набор, а добавляются к уже существующим данным. Если до этого был загружен `pets_batch_1.xml`, общее количество записей становится **110**. Этот сценарий показывает, что система корректно работает с несколькими независимыми XML-файлами и поддерживает последовательную подгрузку данных.


## 12. Вывод

В ходе лабораторной работы была разработана оконная информационная система для учёта питомцев и ветеринарных приёмов.

В результате были реализованы все основные требования задания:
- приложение с графическим интерфейсом;
- архитектура MVC;
- главное окно и дочерние диалоги;
- добавление, поиск и удаление записей;
- хранение данных в базе данных;
- импорт и экспорт XML;
- табличное и древовидное представление данных;
- постраничный вывод;
- unit-тестирование с покрытием выше требуемого уровня.

Разработанная система соответствует поставленным требованиям и может использоваться для демонстрации принципов построения GUI-приложений на Python.
